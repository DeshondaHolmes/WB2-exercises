// sample inputs
var foodCost = 79.25;
var tipPercent = 0.15;

// calculations
var tipAmount = foodCost * tipPercent;

// output
console.log( "The tip on a " + foodCost + " food bill is " + tipAmount);